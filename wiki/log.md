# Wiki Log

Append-only. One entry per ingest or maintenance pass: date, what came in, pages touched.

---

- **2026-07-05** — Wiki seeded by Elliot's interactive session as part of the research-factory build-out. Initial knowledge distilled from the existing raw sources (`state/ideas.md` research evidence across four new-mode rounds, `state/ledger.md` entries 1–14, `portfolio/INDEX.md`). Pages created: 7 concept pages (ad-ruined-casual-games, daily-puzzle-rituals, subscription-economy-backlash, local-first-privacy, solver-verified-puzzle-generation, curated-content-as-product, pure-core-thin-shell) + 5 prototype pages + index. From the next heartbeat on, the workflow's Ingest phase maintains this wiki per WIKI.md.

- **2026-07-05** (heartbeat 22:06, improve — chainfall v0→v1) — Ingested the chainfall v1 design dossier (`research/2026-07-05T2206-dossier.md`, cascade animation feel & timing) and the build/verify outcome. **Created** `game-feel-juice.md` (new concept, no prior home): the stepped-cascade skeleton (clear-flash → collapse → next step), per-step timing budgets (~1.1 s clone baseline, compress ~4–6× for web; Tetris line-clear-delay 400–700 ms as the slow end), chain-multiplier surfacing, `requestAnimationFrame` + per-step timer as the right primitive, and why headless automation can't watch an animation (no vsync → rAF never fires; verify via one captured frame + unit-tested per-step frames, don't grind for deterministic capture). **Updated** `prototype-chainfall.md` (v0→v1: visible stepped cascade, HUD, game-over/restart, 225 tests; re-confirmed the published chain-score table beats the cubic past step 6) and `index.md` (new concept line + chainfall line to v1). Cross-linked game-feel-juice ↔ prototype-chainfall ↔ pure-core-thin-shell ↔ ad-ruined-casual-games. Scope guard respected: the charset/mojibake defect and the verifier's headless-timer over-investment are ENGINEERING lessons (the charset guard went to `test_static_invariants.js` + the template; the over-investment is this heartbeat's evolution) — not ingested here; only the *product* consequence (juice verification can't rely on live animation capture) landed, in game-feel-juice.

- **2026-07-06** (heartbeat 04:06, new — sediment first slice) — Ingested the sediment design dossier (`research/2026-07-06T0406-dossier.md`: event-time policy, adaptive gap-clustering, curated caption payloads) plus the 2026-07-06 app-round ideation (10 concepts) and validation findings (sediment=strong/wow-survives, Palimpsest=weak/novelty-exists). **Created** `prototype-sediment.md` (the metadata-only folder-as-life-diary CLI: `min(birthtime, mtime)` event-time with the Linux `st_birthtime`-`AttributeError` trap and the POSIX-`st_ctime`≠creation trap; synthetic-timestamp detection via near-zero internal variance; the adaptive `g >= max(FLOOR=3d, MULT=8 × median(gaps))` boundary rule from photo-collection event-segmentation literature; the three curated caption signals with DCF camera-prefix reliability; and metadata-only-is-structurally-provable). **Created** `dev-mechanic-transplant.md` (new concept, no prior home — the round's explicit seed "transplant a beloved dev mechanic onto data people already sit on": the two sub-veins folder-is-a-timeline [sediment/Afterdate/Pulse/Daylog/Nightwatch] and literal-git-transplant [cellblame TOP FUTURE PICK / whenbroke / hindsight], the Ghostchars/Palimpsest single-file cousins, and the compounding lesson that Palimpsest died — the vein wins on re-framed DATA but loses on already-inspected FORMATS, so resonance ≠ novelty). **Updated** `local-first-privacy.md` (sediment as the cleanest "local superpower" exemplar; no-content-read is a stronger structural guarantee than no-network; the resonance≠novelty flip that killed Palimpsest), `curated-content-as-product.md` (the pattern holds for APPS — sediment's four curated payloads ARE the product, "the walk is a weekend, this data is the product"), `pure-core-thin-shell.md` (sediment as a fourth app exemplar; re-derivation-for-free bullet extended; new bullet on pushing platform timestamp-mess to the thin-shell seam so the core stays testable), `prototype-subscription-sleuth.md` (its merchant-string alignment trap is now the named precedent for cellblame's align-by-key risk), `prototype-tidy.md` (sediment is tidy's space-vs-time foil; tidy's `EXTENSION_MAP` seeds sediment's category map), and `index.md` (new concept + prototype lines; local-first summary sharpened). Cross-linked dev-mechanic-transplant ↔ prototype-sediment ↔ local-first-privacy ↔ curated-content-as-product ↔ pure-core-thin-shell ↔ prototype-subscription-sleuth ↔ prototype-tidy, both directions. Scope guard respected: the r.1/r.4/r.11/r.12/r.19/r.26 guard mechanics, the `MAX_SPARKLINE_ROWS` unbounded-output fix, and the test design are ENGINEERING concerns (they live in the dossier + lessons.md — r.26 was in fact coined from sediment's `month_sparkline`) — only the PRODUCT/DOMAIN knowledge (the axis-flip insight, the timestamp/gap/caption domain facts, the transplant angle, the killed-idea lessons) landed here.

- **2026-07-06** (heartbeat 06:06, improve — subscription-sleuth price-change detection) — Ingested the price-hike design dossier (`portfolio/subscription-sleuth/research/2026-07-06T0606-dossier.md`: the split-into-two-lines failure mode, the merchant-first ordered level-detection algorithm, the verified US price-hike content table, the Rocket Money differentiator, the noise-case guards). **Updated** `prototype-subscription-sleuth.md` (v1 gains price-change detection; corrected the now-stale "clusters by merchant+amount" description to the merchant-first pass; new domain bullets — price history is a step function so detect by ordered level-detection not amount-clustering, the split/two-line + past-date failure it fixes, the sustain-count guard as the single most important safeguard, annualize from the CURRENT price never the blend, the three noise cases as acceptance tests; the real-Netflix content payload and the $17.99 Netflix/Hulu value-collision watch-out; the refined Rocket-Money-does-alert differentiator; price-hike removed from Open directions). **Updated** `subscription-economy-backlash.md` (jaw-dropping ceiling note marks price-hike detection BUILT; new refinement — the realizable gap is rarely raw *detection* but *transparency/auditability*, because Rocket Money already pushes a price-increase alert; honest-pitch corollary vs. over-claiming). **Updated** `curated-content-as-product.md` (new bullet — real numbers beat plausible numbers; the verified 18-row US price-hike table as an app content payload, the "real merchant-string formats" note now realized for a different field of the same app). **Updated** `local-first-privacy.md` (local buys transparency/auditability, not just privacy — re-derivable table vs. opaque push alert, a distinct edge from "your bytes never left"). **Updated** `pure-core-thin-shell.md` (price-change detection extends the re-derivation-for-free bullet — a step function over date-ordered charges is one more pure fold, both figures route through single `annualize`; new caveat — ordered-in-time segmentation + sustain guard is essential where value-clustering would misread a reverting promo). **Updated** `index.md` (subscription-sleuth line notes price-change detection). Cross-linked subscription-sleuth ↔ subscription-economy-backlash ↔ curated-content-as-product ↔ local-first-privacy ↔ pure-core-thin-shell ↔ settleup, both directions. No new page created — every concept had a home (enriched per schema). Scope guard respected: the rule-11/12/12-corollary/19/22 guard mechanics, the frozen-output regeneration, and the positive `REQUIRED_DOC_SYMBOLS` assertion are ENGINEERING concerns (they live in the dossier + `rules/lessons.md`) — only the PRODUCT/DOMAIN knowledge (the failure mode, the level-detection algorithm, the price-hike content payload, the differentiator refinement, the noise-case domain facts) landed here.

- **2026-07-06** (heartbeat 02:06, improve — settleup `--breakdown`) — Ingested the settleup breakdown design dossier (`research/2026-07-06T0206-dossier.md`, per-person "why do I owe this?" itemization). **Created** `declarative-ledger-vs-running-tab.md` (new concept, no prior home): the data-model choice — immutable declarative ledger vs. mutable running tab — that decides which features are cheap. Splitwise's "itemized/detailed balances" request has been open since Oct 2013, still "under review," because their running-tab model makes it "surprisingly tricky, since people often pay for things out of order"; a declarative ledger has no order problem, so the itemization is a pure re-derivation reconciling `paid − owed == net` by construction. The general principle: pick the data model that makes your differentiating feature a pure re-derivation, and the SaaS's hardest-to-ship feature becomes your cheapest. **Updated** `prototype-settleup.md` (added `--breakdown`/`-b` feature; auditability-closes-the-trust-gap and free-architecture domain notes; two render heuristics — mirror the top table's *words* not raw signed numbers, and show every line even $0.00 because completeness defuses "I don't remember that" disputes), `subscription-economy-backlash.md` (jaw-dropping ceiling note now cites attacking a *capability* gap, not just pricing, with settleup's breakdown as the exemplar), `pure-core-thin-shell.md` (new bullet: a pure core over immutable input can re-explain any output for free), and `index.md` (new concept line + settleup line). Cross-linked declarative-ledger-vs-running-tab ↔ prototype-settleup ↔ subscription-economy-backlash ↔ pure-core-thin-shell ↔ local-first-privacy, both directions. Scope guard respected: the r.4/r.7/r.11/r.12/r.19 guard-budget mechanics, the reconciliation-fixture test design, and the arg-parser flag-stripping order are ENGINEERING concerns (they live in the dossier + lessons.md) — only the *product/domain* knowledge (the unmet demand, the data-model insight, the transparency thesis, the render heuristics) landed here.

- **2026-07-06** (heartbeat 08:06, improve — tidy `--undo --list`) — Repaired by the scribe (this run's workflow Ingest phase left the wiki untouched: `wiki/log.md` had no 08:06 entry and `prototype-tidy.md` still listed `--undo --list` as an Open direction after it shipped). Ingested the undo-history-listing design dossier (`portfolio/tidy/research/2026-07-06T0806-dossier.md`: the `git stash list` precedent for "show me the reversible stack," the absolute-vs-relative-timestamp UX decision, the file-count + `--by-month` recognition signal, the per-entry corrupt-manifest degradation). **Updated** `prototype-tidy.md` (v1 gains `--undo --list`; the one-liner and description now name the read-only undo-stack view; new "inspect-before-reverse ergonomics" domain bullet — `git stash list`'s most-recent-first/top-is-next convention adopted but made friendlier with 1-based ordinals and an explicit `<- next to undo` marker on the provably-same manifest a plain `--undo --apply` reverses; the absolute UTC timestamp as the load-bearing reference anchor reformatted for legibility and kept UTC to match the `Z`-stamped filename, with relative "N hours ago" rejected as a reference-task mismatch that would also fight the frozen-output test; file-count + `--by-month` as the run-recognition signal in the absence of a stash-style message; the defensive-reformat requirement because `timestamp` isn't a validated manifest field; `--undo --list` removed from Open directions, replaced with `--undo --list --json`). No new page created — every concept had a home. Cross-links to `pure-core-thin-shell.md` already present (the pure `undo_list_rows` renderer / thin `run_undo_list` shell split, and the read-only escape-hatch dispatched before config load, per lessons r.17, are the same seam pattern). Scope guard respected: the r.7/r.9/r.17/r.19 guard mechanics, the pure/renderer test seam, and the exit-code contract (list stays exit 0 with an inline `(unreadable: …)` note; a wholly-inaccessible `.tidy/` takes the exit-2 access-error path) are ENGINEERING concerns (they live in the dossier + `rules/lessons.md`) — only the PRODUCT/DOMAIN knowledge (the reversible-stack precedent, the timestamp-display decision, the recognition-signal insight) landed here.

- **2026-07-06** (heartbeat 14:07, new — foldup first slice, game) — Ingested the foldup design dossier (`portfolio/foldup/research/2026-07-06T1407-dossier.md`: the parity-trivialization risk, the fold half-plane reflection + overhang index math, the BFS `solveMinFolds` par oracle, the certified 8-level bank, Unflip/Paperama feel heuristics) plus the 2026-07-06 GAME-round ideation (12 concepts) and validation findings (Foldup=strong/wow-survives revised 8/9/7; Overunder=weak/wow-does-NOT-survive; Proofgrid=strong/crowded). **Created** `prototype-foldup.md` (the fold-the-board-onto-itself XOR puzzle: parity-trivialization means the target must be >1×1 with structure and a validator rejects 1×1; the fold = smaller-half mirrored across the crease with `dest=2*index-1-j`, overhang carries over, board strictly shrinks; the BFS par oracle keyed by content AND dimensions is tiny because the board shrinks and caught two wrong hand-authored pars; ships v0 pending a human feel-playtest). **Created** `novel-puzzle-verbs.md` (new concept, no prior home — the game-round ideation seed twin of `dev-mechanic-transplant`: mine an unclaimed universally-familiar physical gesture as a puzzle's CORE verb; the 12 concepts kept/killed with WHY — Foldup built, Overunder KILLED because switching a crossing over/under is topologically invariant on loop-count so its stated win is a no-op against its verb [arXiv 1908.04073], Proofgrid the strongest unbuilt certified-content sibling; the two banked lessons: search the (verb × win-condition) pair, and verify the win is sensitive to the verb — the game-round form of resonance≠novelty). **Updated** `solver-verified-puzzle-generation.md` (foldup's BFS par oracle as the FIRST shipped instance — certification earns its keep by catching authoring drift not just stamping a badge; constraining the move set to single grid-line creases is what makes an otherwise #P-hard fold-proof tractable; new two-sided "reject the vacuous instance" sub-form — a 1×1 target trivializes regardless of solver), `curated-content-as-product.md` (certified content beats hand-trusted content — the 8-level bank's value is that par is machine-proven, same shape as Proofgrid's build-time forcibility check), `game-feel-juice.md` (new section: the fold as a SINGLE collapse beat — teleport-throws-away-the-moment recurs at one-gesture scale; the FLIP animation; the deferred-render double-input window r.13 and the no-vsync feel-playtest limit both carry over from chainfall's cascade), `pure-core-thin-shell.md` (foldup as a third game exemplar; feasible=9 because a solver-in-the-core certifies fairness from tests alone), `ad-ruined-casual-games.md` (foldup as the worked contrast — the new-VERB route, not the revival route, is the vein's escape hatch from its "finally done right" ceiling), `daily-puzzle-rituals.md` (par-vs-used as the daily hook but hide-par-until-first-solve per the Unflip crowd; Daily Unfold proves the daily fold format), `dev-mechanic-transplant.md` (twin ideation-seed page cross-link + the shared resonance≠novelty / desire≠mechanic discipline), and `index.md` (two new lines + sharpened solver-verified summary). Cross-linked prototype-foldup ↔ novel-puzzle-verbs ↔ solver-verified-generation ↔ curated-content ↔ game-feel-juice ↔ pure-core-thin-shell ↔ ad-ruined-casual-games ↔ daily-puzzle-rituals ↔ dev-mechanic-transplant, both directions. Scope guard respected: the lessons r.4/r.13/r.15/r.21/r.25 guard mechanics, the build.js inline-pin, and the test-suite design are ENGINEERING concerns (they live in the dossier + `rules/lessons.md`) — only the PRODUCT/DOMAIN knowledge (the parity fact, the fold/overhang math, the par-oracle-as-certification, the novel-verb angle, the killed-idea topology lesson, the feel heuristics) landed here.

- **2026-07-06** (heartbeat 08:06, improve — tidy `--undo --list`, second pass / concept-extraction) — The 08:06 entry above recorded the prototype-page ingest; this pass lifts the two facts the dossier itself flagged as "net-new domain knowledge no existing wiki page covered" out of the single prototype page (where the ideation phase would never find them) into a reusable concept. **Created** `reversible-action-stack-ux.md` (new concept, no prior home): the inspect-before-you-reverse pattern for any tool with an undo/snapshot/stash stack — the `git stash list` mental model (most-recent-first, ordering encodes "next," `list`=inspect / `pop`=reverse), being friendlier than git with 1-based ordinals + an explicit next-marker, the load-bearing invariants (marked-next provably == what the reverse verb consumes and derived from the same chronological source not a filename sort; marker follows stack position even when the top entry is corrupt; a per-row recognition signal in the absence of a stash message; read-only-wins-over-mutate and dispatch-before-config-load; per-entry degrade not all-or-nothing), and the absolute-vs-relative timestamp UX rule (reference/reconstruction tasks use absolute full dates, keep the recorded zone and label it, relative time fights deterministic output). **Updated** `prototype-tidy.md` (Connections now points to the new concept page as the shipped instance), `dev-mechanic-transplant.md` (git stash list is itself a transplanted dev mechanic; whenbroke/cellblame's snapshot-series tools would each want this listing), `prototype-sediment.md` (its era/event dates are reconstruction anchors, so they fall under the same absolute-timestamp-for-reference rule), and `index.md` (new concept line under Mechanics & product patterns). Cross-linked reversible-action-stack-ux ↔ prototype-tidy ↔ dev-mechanic-transplant ↔ prototype-sediment ↔ pure-core-thin-shell ↔ local-first-privacy, both directions. No contradiction with the prior entry — this compounds it. Scope guard respected: still only PRODUCT/DOMAIN knowledge (CLI UX conventions, timestamp-display UX); the test/exit-code/guard mechanics stay in the dossier + `rules/lessons.md`.
