# Directive Inbox

Write directives below the divider, one bullet each. The next heartbeat obeys them, then moves them to `archive.md`. Lines prefixed `standing:` stay here and apply to every heartbeat until you delete them.

Examples:
- `standing: focus every heartbeat on <prototype-name> until I say otherwise`
- `add a --json output flag to <prototype-name>`
- `next new prototype: something for organizing screenshots`

---

_(inbox empty)_
