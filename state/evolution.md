# Evolution Log

One factory self-improvement per heartbeat, applied by the heartbeat session after the product work (see the Evolution section of FACTORY.md for the rules). Every entry starts `pending`; the NEXT heartbeat validates it (did the run pass cleanly through what it touched?) and marks it `validated`, or reverts the commit and marks it `reverted` with the reason.

## Proposed backlog (pick from the top, reorder freely with reasons)

1. Ledger rollup: when `state/ledger.md` passes ~30 entries, move the oldest into `state/ledger-archive.md` and keep a one-line-per-heartbeat summary table, so heartbeat sessions read less context. *(Deliberately deferred by the 2026-07-05 Elliot-session build-out: the ledger held ~14 entries, under the threshold; now at 19 and climbing — getting closer. Mild friction already showing: reading the tail with `tail -n 90` overflows the tool output at ~43KB because entries are long narratives.)*
2. Weekly digest: on the first heartbeat after Sunday midnight, write `state/digest-<date>.md` summarizing the week (prototypes touched, graduations, evolution changes, open issues) for Elliot to skim.
3. Hub thumbnail per card: once playtest screenshots accumulate under `reports/assets/`, surface a thumbnail per prototype on the hub front page (and/or dashboard cards).

_(Done — removed from backlog: "Validation-agent StructuredOutput robustness (VALIDATE-agent variant)" — implemented this 2026-07-06T08:06 heartbeat, see the Log entry at the bottom. Fixed by applying the now-VALIDATED dossier root-cause fix to the sibling: the two large URL-laden free-text fields `prior_art`/`resonance_evidence` were made OPTIONAL + size-steered, so a payload-size XML mangle can no longer burn the retry cap and drop a candidate; all small decision fields the selector depends on — `novelty`, `wow_survives`, `revised`, `verdict` — stay required, honoring the item's "do not weaken the strong/weak decision" red line.)_

_(Done — removed from backlog: "Dashboard depth: per-prototype 'heartbeats spent' counter + last-updated stamp per card" — implemented this 2026-07-06T04:06 heartbeat, see the Log entry at the bottom. It was the preferred next pick once the 2026-07-06 footer-enumeration fix — validated this same run — tightened the scribe's dashboard instruction so the new per-card facts land on a non-rotting enumeration.)_

_(Done — removed from backlog: (a) "Rotate research lenses" implemented in commit `0a4c0bb` (2026-07-05T16:06), validated by the 2026-07-05T18:06 chainfall run; (b) Elliot's approved pipeline-review item #1 "playtest lens" implemented in the 2026-07-05T20:10 heartbeat; (c) review items #2–#5 plus the scribe/coroner hygiene item all implemented directly by Elliot's interactive session on 2026-07-05 — see the "Elliot-directed build-out" log entry below and `directives/archive.md`. The old "Idea memory" item is subsumed by the new Ideation/Validate pipeline, which reads and writes `state/ideas.md` including killed-idea rationales.)_

## Log (newest at bottom)

### 2026-07-04 — Scribe re-derives final numbers instead of trusting the pre-coroner builder summary
- **Change:** `workflows/heartbeat.js` — the scribe prompt now labels the builder summary as PRE-CORONER, explains that the coroner runs after the builder and can change tests/sample/output, and instructs the scribe to re-run the prototype's test command and cold-start run command and record the ACTUAL observed figures rather than copying counts from the builder summary.
- **Why:** Friction observed this heartbeat: the coroner's final commit (`2f630b2`) added invariant tests and expanded `subscription-sleuth`'s sample, so the true state was 49 tests / 9 recurring charges / $1,098.51, but the scribe copied the builder's pre-coroner numbers (45 tests / 7 charges / $828.54) into the ledger. I had to hand-repair four figures. This closes the class at the source so the scribe stops recording stale post-build figures.
- **Commit:** 9813fb5
- **Status:** validated — both scribes this heartbeat (2026-07-05T00:01 tidy, 2026-07-05T00:19 subscription-sleuth) did exactly what this change instructs: each re-ran the prototype's own test command instead of trusting the PRE-CORONER builder summary, caught the stale count the coroner had changed (tidy 42→46; subscription-sleuth 55→56), and recorded the live figure. The behavior fired cleanly twice and needed no manual repair.

### 2026-07-05 — Document how to handle a directive that spans multiple prototypes
- **Change:** `FACTORY.md` — added a "Multi-prototype directives" sub-note under Heartbeat-procedure step 4: when a directive names several prototypes, run the workflow once per named target, sequentially (never in parallel — the scribes rewrite the shared ledger/INDEX/dashboard and would clobber each other), each run with its own timestamp and report, and treat the set as one heartbeat's directive-driven unit (so one scheduled heartbeat can append more than one ledger entry, which the "heartbeats run" stat counts).
- **Why:** Friction observed this run. The active directive named both `tidy` and `subscription-sleuth`, but the workflow takes a single `target`/`prototype_dir`. I had to derive on the fly that this means two sequential runs and that parallel runs would collide on shared state — guidance that belongs in the constitution so the next multi-target directive is mechanical, not re-derived. Fair game (policy wording), not `heartbeat.js` and not the Hard constraints section.
- **Commit:** 0a07147
- **Status:** validated — this run read FACTORY.md step 4 (including the new "Multi-prototype directives" sub-note) to decide the workflow args and passed through it cleanly: with an empty inbox and no directive, the note correctly did NOT apply and did not muddy the single-target new-mode decision. The added wording is purely additive documentation and coexists correctly with a single-target run. (The multi-target code path itself wasn't exercised this run — no multi-prototype directive arrived — but the touched file was consumed without friction, which is the validation bar.)

### 2026-07-05 — Scribe pins the ledger-heading status to the INDEX status
- **Change:** `workflows/heartbeat.js` — rewrote the scribe prompt's instruction 1. It now tells the scribe to first decide ONE status on the FACTORY.md ladder (idea/v0/v1/polish) from what actually shipped, then use that same value in BOTH the ledger heading and the INDEX row, and it pins the exact ledger heading format `## <ts> — <mode> — <prototype> (<status>)` while forbidding placeholder words ("prototype"/"new"/"shipped").
- **Why:** Friction observed this run (a manual repair the scribe needed). The scribe prompt never specified the ledger heading format, so groupline's scribe wrote the heading as `… — groupline (prototype)` even though it correctly assigned status `v0` in INDEX.md. I had to hand-repair the heading to `(v0)`. This closes the divergence class at the source: heading status and INDEX status are now instructed to be the same single value in the same prompt.
- **Commit:** caf9cbf
- **Status:** validated — this run's scribe passed cleanly through the pinned instruction: it wrote the ledger heading as `## 2026-07-05T06:06:58+01:00 — improve — groupline (v1)`, which matches the pinned `## <ts> — <mode> — <prototype> (<status>)` format exactly with a real ladder status (`v1`, no placeholder word), and the INDEX.md row uses the SAME status `v1`. No manual heading repair was needed this heartbeat (contrast the groupline-v0 run, where the un-pinned prompt produced `(prototype)` and I hand-repaired it). The behavior fired correctly on the first improve-mode run after the change.

### 2026-07-05 — Isolate each concurrent verifier's scratch dir per-run (encode lessons rule 5)
- **Change:** `workflows/heartbeat.js` — added one line to the adversarial-verifier agent prompt (the three-lens `verify()` fan-out) instructing each verifier that when it creates scratch files/folders to exercise the tool, it must make its OWN fresh per-run temp dir (`mktemp -d` / `tempfile.mkdtemp`) unique to that verifier, never a shared or fixed scratch path, because the three verifiers run CONCURRENTLY and a shared path cross-contaminates their fixtures and yields false failures (lessons rule 5).
- **Why:** Backlog item (was #2), reordered above #1 with reason: it is exercised on EVERY heartbeat (the verify fan-out always runs), whereas the top backlog item "rotate research lenses" only applies to the less-frequent new-mode runs, so this one is both higher-frequency-valuable and validatable on the very next heartbeat. The hazard is concrete: `verify()` runs three verifier agents via `parallel()`, each told to "run the run command yourself" and "attack the acceptance criteria with edge cases"; for a file-manipulating prototype (e.g. `tidy`) three verifiers building fixtures in one shared scratch path would collide — exactly rule 5's cross-contamination class — producing false-alarm blocking issues. This groupline run didn't trip it (its verifiers ran Node core tests with no on-disk fixtures), so it's latent, not yet-observed friction; encoding it now closes the class before the next file-tool improve target reaches the verifiers. Small, additive, reversible; `node --check` passed. Fair game (workflows/heartbeat.js), not the Hard constraints section, and heartbeat.js did not crash last run so it was safe to edit.
- **Commit:** 859f03e
- **Status:** validated — this heartbeat exercised the exact scenario the change targets: an IMPROVE run on `tidy` (a file-manipulating prototype), so all three adversarial verifiers created on-disk fixtures. The workflow transcript confirms each of the six verifier agents (two verify rounds × correctness/usefulness/quality) made its OWN fresh `mktemp -d`/`tempfile.mkdtemp` scratch dir — the paths are all distinct (`tmp.1MAArdM9WB`, `tmp.8Z8sAwt8iz`, `tmp.8plom5c3nW`, `tmp.4BGVgfJkmZ`, `tmp.cRzC4yHDk3`, `tmp.TrYiVaylF6`, …), no two shared a path. No cross-contamination false-failure appeared (the single blocking issue raised — the null-byte config crash — was a genuine bug, later fixed by the coroner). The "cross-contaminate/shared scratchpad" strings in the transcript are the verifiers quoting the instruction they were given, not collision reports. The added instruction fired cleanly on the first file-tool improve target to reach the verifiers.

### 2026-07-05 — Scribe treats the shipped flag + blocking issues as PRE-CORONER and re-confirms them against committed code
- **Change:** `workflows/heartbeat.js` — the scribe prompt previously labelled only the builder's FIGURES as pre-coroner (from evolution 9813fb5); it now also labels the `shipped` flag and the blocking-issue list it is handed as pre-coroner, explains that the coroner frequently RESOLVES the last blocking issue itself by committing the fix/guard (which flips the true status to shipped), and instructs the scribe to reproduce EACH listed blocking issue against the CURRENT committed code — recording the run as shipped (noting the coroner resolved it) when none still reproduce, or as still-blocked only for issues it can personally still reproduce.
- **Why:** Friction observed this run. The workflow computes `shipped`/`openIssues` at line 165 BEFORE the Coroner phase, so when the coroner fixed the sole remaining blocking issue (the null-byte config-category crash, coroner commit `99a64c8`), the workflow still returned `shipped: false` with the already-fixed issue in `openIssues`. That misled the heartbeat session at close-out (I had to dig through the git log, ledger, and a fresh repro to establish the run had actually shipped) and, more importantly, handed the scribe a `shipped: false` + a blocking issue as if authoritative. This run's scribe recorded the correct status anyway (it re-verified and reasoned well), but the prompt never told it the status could be stale — so the correct outcome was luck, not instruction. This closes that gap at the point that matters most: the durable ledger/INDEX status. Fair game (workflows/heartbeat.js); heartbeat.js did not crash last run or this run, so it was safe to edit; `node --check` passed. Purely additive prompt text, no control-flow change — trivially reversible.
- **Commit:** 2bc34fa
- **Status:** validated — this heartbeat's scribe consumed the changed prompt and passed through it cleanly. It treated the builder's figures AND the shipped flag as PRE-CORONER: re-ran `./run_tests.sh` live (catching the builder summary's stale "92 tests" → actual **102**) and reproduced each pre-coroner blocking/error case (empty/whitespace-only/garbage/BOM-only files, payer-not-in-participants, missing file, `" A "` name) against the CURRENT committed code before recording. The ledger entry explicitly says "I then reproduced EACH of the builder's PRE-CORONER blocking/error cases against the current committed code, and all are clean" — exactly the instruction. shipped was already true pre-coroner this run (a `new` run whose verify passed), so the coroner-flips-false-to-true branch wasn't the literal path, but the touched prompt was consumed with zero friction and needed no manual repair, which is the validation bar (same bar applied to the multi-prototype-directives entry).

### 2026-07-05 — Coroner reports which blocking issues it resolved so the workflow RETURN VALUE is post-coroner-accurate
- **Change:** `workflows/heartbeat.js` — the fire-and-forget coroner agent now returns a small `CORONER` StructuredOutput schema (`{ resolved_blocking: string[], still_blocking: string[] }`). Its prompt keeps the durable-check work as the stated PRIMARY job, then as a separate final step is handed the post-fix-round `blocking` list and asked which of those its committed fixes resolved vs. still reproduce against committed code. After the Coroner phase the script computes `const stillBlocking = coronerResult ? coronerResult.still_blocking : blocking` and the `return` now uses `shipped: stillBlocking.length === 0, openIssues: stillBlocking`.
- **Why:** Top of the Proposed backlog, and the natural follow-on to the just-validated `2bc34fa`: that fixed the SCRIBE half of the pre-coroner-staleness problem (the durable ledger), but the workflow's `return` still computed `shipped`/`openIssues` at line ~165, BEFORE the Coroner phase. When the coroner resolves the last blocking issue by committing the fix (e.g. the 2026-07-05T08:07 tidy run's null-byte crash, coroner commit `99a64c8`), the return said `shipped: false` with an already-fixed issue — which misled the heartbeat session at close-out (I had to dig through git log to establish the run actually shipped). **Deliberately narrowed from the backlog text:** the backlog said thread the corrected value "into both the scribe input and the return," but the scribe already re-derives correctly from pre-coroner data (that's what `2bc34fa` established and this run validated), and feeding it the already-filtered post-coroner list would REDUCE its cross-check rigor (fewer issues to reproduce). So the scribe input is left exactly as-is; only the return contract is fixed. Failure mode is safe: even if the coroner mislabels an issue, the durable ledger is still protected by the scribe's independent re-derivation — only the heartbeat session's close-out reads the return.
- **Commit:** 5470d9e
- **Status:** validated — this heartbeat's coroner consumed the new CORONER StructuredOutput schema and the return computed from it, cleanly. The coroner returned `{"resolved_blocking":[],"still_blocking":[]}` (a clean improve run with no pre-coroner blocking issues), the script computed `stillBlocking = coronerResult.still_blocking = []` at line 197, and the workflow `return` at line 213 used `shipped: stillBlocking.length === 0` → `shipped: true, openIssues: []` — which is exactly the accurate post-coroner state (70 tests green, feature demonstrated, working tree clean at `6f85a83`). The heartbeat session's close-out read an accurate `shipped: true` with no already-fixed issue lingering in `openIssues`, so no git-log archaeology was needed (contrast the 2026-07-05T08:07 tidy run that motivated the change). The literal "coroner-resolves-the-last-blocking-issue → flips false to true" branch wasn't exercised (nothing blocked this run), but the touched code path — the schema, the `stillBlocking` computation, and the return using it — was consumed with zero friction and produced a correct return, which is the validation bar applied to prior entries.

### 2026-07-05 — Clarify mode policy for the all-prototypes-≥-v1 regime (cadence, not the gate, governs)
- **Change:** `FACTORY.md` — expanded Heartbeat-procedure step 3's **new** bullet. It now states that the ~1-new-per-3-heartbeats cadence, not the numeric "fewer than 2 below v1" gate, is the real governor of mode; that when the gate is trivially open because EVERY prototype is already ≥ v1 (no below-v0/below-v1 improve target exists) the session should still default to **improve** (round-robin polish, advancing a v1 toward polish) and pick **new** only when the last ~3 ledger entries contain no `new` run; and that the mission is to graduate prototypes to completion, so the all-≥-v1 regime should deepen an existing prototype rather than sprawl in breadth.
- **Why:** Friction I actually navigated this run. The portfolio just reached the state where ALL four prototypes are at v1 and none is below — so the `new` gate ("fewer than 2 below v1") is trivially satisfied (0 < 2) and, read literally, permits spawning a new prototype every heartbeat. The only brake is the soft "~1 per 3 heartbeats" line, and step 3 never said how to weigh the wide-open gate against that cadence. I had to derive on the fly that improve is the default here (last heartbeat was a `new` run shipping settleup, so new isn't due) — reasoning that belongs in the constitution so the next session applies it mechanically instead of re-deriving it. The factory will sit in this all-≥-v1 regime for many heartbeats until something reaches polish, so the clarification is load-bearing now, and it protects the graduate-toward-polish mission against breadth-sprawl. Fair game: FACTORY.md policy wording. It does NOT change the 3-hour schedule cadence (Elliot-only, not fair game) and does NOT change the ~1/3 product ratio — it only clarifies how to APPLY that existing ratio when the gate is uninformative. Small, additive, reversible (one bullet expanded); no `heartbeat.js` touched, so no crash risk.
- **Commit:** 030e907
- **Status:** validated — this heartbeat consumed the expanded step-3 wording to decide mode and passed through it cleanly. The portfolio is in exactly the all-≥-v1 regime the clarification targets (all four prototypes ≥ v1, no below-v1 improve target), so the numeric `new` gate was trivially open (0 < 2); applying the new rule literally, I checked the last ~3 ledger entries, found a `new` run (settleup at 2026-07-05T10:07), concluded a new prototype was NOT due by the ~1/3 cadence, and defaulted to **improve** (round-robin polish → groupline). That is precisely the behavior the bullet prescribes, produced mechanically without re-derivation, and needed no manual repair. The wording is purely additive policy and coexists correctly with the rest of step 3.

### 2026-07-05 — Define round-robin-polish TARGET selection (least-recently-advanced), the companion to the mode clarification
- **Change:** `FACTORY.md` — extended Heartbeat-procedure step 3's **improve** bullet. It now defines how to pick the prototype within the **round-robin polish** tier (the all-≥-v1 regime where every prototype qualifies): choose the one whose most recent substantive `improve`/`new` ledger entry is OLDEST (least-recently-advanced), so polish attention rotates fairly across the portfolio instead of repeatedly deepening the same prototype; ties break by `portfolio/INDEX.md` row order (topmost first); and it clarifies that "round-robin" means fair rotation recomputed from the ledger each heartbeat, not a fixed cycle.
- **Why:** Friction I actually navigated this run, and the natural companion to the just-validated `030e907`. That entry clarified the MODE decision (improve vs new) in the all-≥-v1 regime but said nothing about the TARGET decision WITHIN improve: step 3's tier list ends in "round-robin polish" with "round-robin" left undefined, and in the all-≥-v1 regime EVERY prototype is a round-robin-polish candidate, so the ordering is load-bearing yet unwritten. I had to derive on the fly that the fair pick is the least-recently-advanced prototype — ordering the four by their most-recent ledger entry (groupline 06:06 < tidy 08:07 < settleup 10:07 < subscription-sleuth 12:06) and picking groupline — reasoning that belongs in the constitution so the next session applies it mechanically instead of re-deriving it. The factory will sit in this all-≥-v1 regime for many heartbeats, so like `030e907` this clarification is load-bearing now and protects the graduate-toward-polish mission against always-polishing-the-loudest-one. Fair game: FACTORY.md policy wording — it does NOT change the schedule cadence (Elliot-only) or the ~1/3 product ratio, and touches no `heartbeat.js`, so no crash risk. Small, additive, reversible (one bullet extended).
- **Commit:** f9049d8
- **Status:** validated — this heartbeat consumed the new round-robin-polish TARGET rule and passed through it cleanly. With an empty inbox, all four prototypes ≥ v1, and no `new` due (the last 3 ledger entries at decision time included settleup's `new` run at 10:07), the session was in the exact all-≥-v1 round-robin-polish regime the rule governs. Applying it mechanically: I ordered the four by their most-recent substantive ledger entry — tidy 2026-07-05T08:07 < settleup 10:07 < subscription-sleuth 12:06 < groupline 14:06 — and picked the least-recently-advanced, **tidy**, exactly as the rule prescribes (no tie, so the INDEX-row tiebreak wasn't needed). The pick was derived directly from the ledger without re-derivation, produced a coherent improve run (`--json` shipped, v1 held), and needed no manual repair. Purely additive policy wording that coexisted correctly with the rest of step 3.

### 2026-07-05 — Rotate new-mode research lenses (timestamp-seeded window over a 6-lens pool)
- **Change:** `workflows/heartbeat.js` — the new-mode Research phase no longer hard-codes three fixed research lenses per product type. Each type (game / app) now has a POOL of six distinct angles, and the run picks a rolling window of three: the start offset is a deterministic function of the run timestamp `TS` (a polynomial rolling hash, `acc = (acc*31 + charCode) % 1e9+7`, then `% pool.length`), and the three lenses are `pool[(offset+0..2) % 6]`. Added three new game angles (genre-mashup, accessibility-and-calm, nostalgia-revival) and three new app angles (privacy-first, dev-workflow, data-janitor) alongside the existing three of each. A `log()` line prints the chosen window so each run's angle set is visible in `/workflows`.
- **Why:** Top of the Proposed backlog (item #1: "vary the three new-mode research lenses between runs so consecutive new prototypes don't all come from the same angles"). The first four prototypes were all researched through the same three fixed lenses per type, which biases the idea funnel toward the same handful of angles every new run — narrowing portfolio variety, which the mission explicitly values ("small, useful, interesting… genuinely fun"). **Deliberately realized statelessly**, diverging from the backlog text's "keep a small lens-history in `state/`": the Workflow sandbox has no filesystem, `Date`, or `Math.random` (they throw), so a persisted lens-history file would require an extra agent round-trip just to read/write it. A timestamp-seeded rotation achieves the same "vary between runs" intent self-contained in the script — `TS` differs every heartbeat, so the window moves — and is trivially reversible. I first tried a plain char-code SUM for the seed and caught (via a `node -e` preview) that it mixes poorly — three same-day timestamps collided on one offset; the rolling hash fixed that (offsets spread 0,1,1,4,2,5,0 across successive candidate times). Timed to validate on the very next heartbeat: after this run the last three ledger entries are all `improve`, so a `new` run is due by the ~1/3 cadence — the changed code path will actually run next.
- **Commit:** 0a4c0bb
- **Status:** validated — this heartbeat was a new-mode (game) run, so the rotating-lens code path actually ran. The Research phase logged `research lenses (window 0/6): beloved-but-ruined, daily-ritual, novel-mechanic` — a deterministic window derived from this run's timestamp — and research completed cleanly through the pool, gathering 14 candidate ideas from which `chainfall` was selected. I independently replayed the exact rolling-hash offset over the real/plausible heartbeat timestamps and confirmed the rotation genuinely varies rather than sticking on one angle set: offsets came out 3, 3, 1, 5, **0** (this run), 2, 5 — spanning five distinct windows, so the mechanism rotates as intended. This run's timestamp happened to hash to offset 0, which coincides with the three ORIGINAL pre-evolution lenses, so the diversity benefit wasn't visibly exercised THIS run (the output is indistinguishable from the old fixed set); but that is a legitimate rotation outcome (1-in-6), not a failure — the next new-mode heartbeat lands elsewhere (offset 2 at ~21:06). The changed code was consumed with zero friction and produced a correct, deterministic result, which is the validation bar. Not reverted.

### 2026-07-05 — Define the status ladder for single-file HTML-canvas games (feel unverifiable at file:// caps the first slice at v0)
- **Change:** `FACTORY.md` — added a "Status-ladder note for single-file HTML-canvas games" paragraph right under the Mission's stage-ladder definition. It states that the **v1** bar "genuinely usable" requires the prototype's PRIMARY interface to have actually been exercised; that for a game whose feel lives in a canvas/animation layer automation cannot drive at a `file://` origin (Playwright/browser-harness both block it, per lessons rule 14), that feel is part of "usable", so a first slice whose logic core is fully tested but whose interactive feel has NOT been confirmed in a real browser is **v0**, graduating to **v1** only once the feel is verified in a real browser and the game-facing shell (HUD, game-over/restart) is present. It cites the `groupline` and `chainfall` precedents, contrasts the CLI-app case (whole interface exercised by tests → can reach v1 on the first slice, as `subscription-sleuth`/`settleup` did), and tells scribes to assign a new single-file-canvas game's first slice v0 unless its feel was confirmed in a real browser this heartbeat.
- **Why:** Friction I actually navigated AND manually repaired this run — the exact "a manual repair the scribe keeps needing" case the Evolution step names. This heartbeat's scribe assigned `chainfall`'s first slice status **v1**; I had to repair it to **v0** across the ledger heading, `portfolio/INDEX.md`, and `portfolio/dashboard.html` (plus the "at v1 or beyond" stat 5 → 4). The scribe prompt and FACTORY.md gave no guidance on how the status ladder applies to a single-file HTML-canvas game whose feel can't be exercised at `file://`, so the scribe over-claimed v1 while the directly-analogous `groupline` first slice had shipped v0 — an inconsistency in the games ladder. This is a RECURRING class (every new single-file game hits the file:// feel-verification gap), so codifying the rule in the constitution closes it at the source: the next new-game scribe assigns v0 mechanically instead of me re-deriving and repairing it, and the label stays consistent across games. Put in FACTORY.md (read by the scribe AND the verifier AND future heartbeat sessions via the workflow CTX) rather than only the scribe prompt, because it is a ladder DEFINITION, and left `heartbeat.js` untouched (lower risk — no crash surface). Fair game: FACTORY.md policy wording; NOT the Hard constraints section, NOT the schedule cadence. Small, additive, reversible (one paragraph).
- **Commit:** 9d1b47d
- **Status:** validated — this heartbeat consumed the FACTORY.md status-ladder note without friction and needed no status repair. The literal "new single-file-canvas game → v0" branch wasn't the exact path (this was an IMPROVE run on the already-`polish` groupline, not a new game), but the note's ADJACENT clause — a canvas game's feel is verified "in a real browser" as the graduation gate — was exercised head-on: this heartbeat's directive-driven Playwright playtest over a localhost server RETROACTIVELY confirmed groupline's interactive feel (the exact feel its v1/polish status had only assumed), and the scribe correctly kept it `polish` with no repair. The partial signal the note named also held: no heartbeat re-repaired chainfall's status (it stayed `v0`). Purely additive policy wording that coexisted correctly with the rest of the Mission section; consumed cleanly, which is the validation bar applied to the prior policy-wording entries (`030e907`, `f9049d8`). The definitive new-canvas-game assignment path will still get its first literal exercise on the next new-game run, but nothing about that is left uncertain by this run.

### 2026-07-05 — Game verify gets a real-browser "playtest" lens (localhost server + isolated-profile Playwright)

- **Change:** `workflows/heartbeat.js` — the adversarial `verify()` fan-out now builds its lens list in a `verifyLenses` array and, when `PTYPE === 'game'`, pushes a FOURTH **"playtest"** lens. That lens instructs the verifier to start a localhost static server from the prototype dir (`python3 -m http.server`), drive the game over `http://localhost` with Playwright — writing a self-contained throwaway Node script that launches an **isolated temp-profile** Chrome (`chromium.launchPersistentContext(mktemp, {channel:'chrome', headless:true})`) to sidestep the shared MCP browser-profile lock, and dispatching clicks IN-PAGE because single-file games re-render the whole DOM and detach handles — actually PLAY a win and a loss plus the primary controls (incl. a 375px check), capture 3–6 screenshots into `reports/assets/<slug>/`, and assert ZERO console/page errors and ZERO external (non-localhost) requests. It keeps the static offline-from-disk invariant test as the `file://` guard (explicitly "do not delete or weaken it"), and DEGRADES GRACEFULLY (verdict pass + a non_blocking note) if Playwright can't be driven in the environment, so a missing browser never hard-fails a game heartbeat. Apps are unchanged (still 3 lenses); the shared verifier prompt's "the three verifiers" wording became "the verifiers" (now 3 or 4).
- **Why:** Top of the Proposed backlog after this heartbeat's directive-seeding — Elliot's approved 2026-07-05 pipeline-review item #1. Games' whole value is FEEL, and lessons rule 14 established that the `file://` offline claim can't be exercised by automation, so games have been shipping with their interactive feel unverified by any repeatable check (groupline and chainfall both shipped their first slices at v0 for exactly this reason). This heartbeat's own directive-driven groupline playtest — localhost `http.server` + an isolated-profile Playwright script driving the real UI through a full win, a full loss, the picker board, Shuffle/Deselect, and Copy-results, capturing screenshots with zero external requests and zero JS errors, and surfacing the real (favicon-404) gap the static tests never could — is the worked precedent that proves the lens is both feasible and valuable; encoding it turns that one-off into every future game verify. Deliberately scoped to `verify()` only (additive, game-gated, graceful-degrading) so it can't regress app runs or hard-fail on environment. Fair game: `workflows/heartbeat.js`; not the Hard constraints, not the schedule. The last workflow (this heartbeat's groupline run) completed cleanly (`shipped: true`), so editing `heartbeat.js` was safe; `node --check workflows/heartbeat.js` passed after the edit.
- **Commit:** (this heartbeat's factory-repo commit — see git log)
- **Status:** validated (with a follow-on fix) — the next GAME heartbeat was this 2026-07-05T22:06 `improve` run on chainfall (v0→v1), and the 4th "playtest" lens ran exactly as designed: the verifier started a `python3 -m http.server` on localhost, wrote a self-contained isolated-temp-profile Playwright script, drove the real chainfall UI over `http://127.0.0.1`, played drops/cascades, and captured screenshots into `reports/assets/chainfall-v1/` (`01-fresh-board.png`, `02-cascade-flash.png`, `03-flash-pulse.png`). It delivered real value the static tests could not: its `01-fresh-board.png` is what surfaced the charset/mojibake defect the coroner then fixed. So the lens FUNCTIONS and is not reverted. It did, however, expose one gap — the lens had NO stop-budget and NO guidance about headless animation-capture limits, so the verifier sank ~2¾h fighting headless Chromium's missing vsync / Playwright timer-throttling trying to film a deterministic cascade, and ultimately hung (the heartbeat session stopped it and salvaged). That gap is closed by THIS heartbeat's evolution (the animation-capture-reality + hard-stop-budget note added to the same lens, below), which is the correct "validated but refined" outcome rather than a revert.

### 2026-07-05 — Concurrency guard: one heartbeat at a time (lock + defer)

- **Change:** `FACTORY.md` — added a "Before you start — one heartbeat at a time (concurrency guard)" subsection at the top of the Heartbeat procedure; `.gitignore` — added `state/heartbeat.lock`. The guard: before launching the workflow, a heartbeat checks `state/heartbeat.lock`; if a lock younger than 3h (the schedule interval) exists, another heartbeat is in progress and this one **DEFERS** (no workflow, no shared-file writes) with a one-line note; otherwise it writes the lock (reclaiming a ≥3h stale/runaway lock and noting the anomaly), proceeds, and removes the lock after its final commit. A second corroborating signal (uncommitted tree / very-recent `ledger`·`evolution` mtime) tells a session never to overwrite another's in-flight work. The lock is gitignored transient state with staleness-reclaim, so it can never deadlock the factory.
- **Why:** THE friction of this very run, observed first-hand and severe. This heartbeat (assigned `04:06:58`) launched its groupline workflow, whose build agent then BLOCKED ~15h (the whole workflow ~17h) on a concurrency conflict — a parallel heartbeat was building the same target. During those 17h, ~8 other scheduled heartbeats (`06:06` → `20:10`) ran concurrently and completed, advancing groupline v0→v1→polish and the whole portfolio. My workflow finally returned to find its product target already built (the builder correctly made no net change), salvaging only a docs commit (groupline `11d9b1e`) plus two coroner lessons corollaries (factory `4375e70`/`93fe85c`). Worse, the shared meta-files became a battleground: my scribe appended an out-of-order `04:06` "backfill" ledger entry, and the concurrent `2026-07-05T20:10` session then REMOVED it as a "phantom" and corrected the dashboard heartbeat count 15→14 (committed in `fe8e734`). Multiple heartbeats clobbering one ledger/INDEX/dashboard, duplicating work, and reconciling away each other's entries is a systemic defect the factory had no guard against. The lock/defer protocol serializes the common case (sub-interval runs) and degrades safely for pathological long runs. Fair game: FACTORY.md policy wording + a gitignore line; NOT the Hard constraints section, and NOT the schedule cadence (the 3h interval is unchanged — only how a heartbeat behaves when it finds a predecessor still running). `heartbeat.js` deliberately left untouched — my workflow had the 17h anomaly, so heartbeat.js was off-limits this round per the Evolution rules, and the guard belongs at session startup (before the workflow), which is the heartbeat session's job, not the workflow's. Small, additive, reversible.
- **Note on this run's other two steps (product + previous-evolution validation), given the concurrency anomaly:** (a) **Product** — no separate product ledger entry was added. The share-grid slice this heartbeat was assigned had already been built and shipped by the concurrent runs, and the `2026-07-05T20:10` session authoritatively recorded groupline's state and reconciled my out-of-order entry away; re-adding it would recreate the phantom that session just cleaned up. My durable contributions (groupline `11d9b1e` docs + the positive `REQUIRED_DOC_SYMBOLS` docs-vs-code guard; factory lessons rules 12/14/24 corollaries in `4375e70`/`93fe85c`) are permanent ancestors of the current HEAD. (b) **Previous evolution** — the newest prior entry (the "playtest lens", pending) POSTDATES my workflow: the 20:10 session added it to `heartbeat.js` after my `04:06` launch, so my workflow ran the pre-playtest 3-lens `verify()` and never exercised it — I therefore cannot validate it and left it pending for the next GAME heartbeat, exactly as its own status says.
- **Commit:** the `Evolution: concurrency guard — one heartbeat at a time (lock + defer)` factory-repo commit (see git log — it is the commit directly after `fe8e734`).
- **Status:** validated — this 2026-07-05T22:06 heartbeat consumed the guard at startup and through a long run, cleanly. At start it checked `state/heartbeat.lock` (none present), corroborated with a clean working tree + no running workflow/task, wrote the lock with the current ISO timestamp, and proceeded — exactly the no-fresh-lock branch. Then the guard proved load-bearing: the workflow's playtest verifier ran ~2¾h (approaching the 3h staleness threshold), and the session applied the guard's own staleness logic in reverse — since it had FIRST-HAND evidence the heartbeat was genuinely still in progress (the verifier transcript was actively being written), it REFRESHED the lock timestamp to prevent a scheduled sibling heartbeat from reclaiming the lock as ">=3h abandoned" and clobbering the shared meta-files mid-salvage. That is precisely the "never let concurrent heartbeats clobber the ledger/INDEX/dashboard" outcome the guard exists for, and it worked. The literal "a second heartbeat finds a fresh lock and DEFERS" branch still hasn't been exercised (no sibling started during this run), but the lock-claim, the staleness-threshold reasoning, and the lock's role in serializing shared-file access were all exercised without friction, which is the validation bar applied to prior entries — and the lock was removed after the final commit as specified.

Entry format:

### <ISO date> — <short title>
- **Change:** what was modified (files)
- **Why:** the friction or proposal it addresses
- **Commit:** <hash in the factory repo>
- **Status:** pending | validated | reverted (+ reason)

### 2026-07-05 — Elliot-directed build-out: research factory (ideation/validate/dossier/wiki), Factory Hub, templates, checklist lessons, status.json (applied by Elliot's interactive session, outside the one-per-heartbeat rule at his explicit request)

- **Change:** (1) `workflows/heartbeat.js` — new-mode Research replaced by **Ideation** (6 generative angles/type, TS-rotated window of 3, reads wiki + ideas backlog, invents wow-first) → **Validate** (triage shortlist, then per-candidate web research: novelty/resonance/feasibility, wow_survives) → selector (wow-first, backlog fallback when no wow survives); a **Dossier** phase for EVERY build (writes `portfolio/<proto>/research/<slug>-dossier.md`; builder must consume it and report `dossier_findings_consumed`; usefulness verifier spot-checks); an **Ingest** phase compounding each run's research into `wiki/`; coroner budget (fix + exactly ONE durable check) + commit hygiene (prototype-repo only); scribe: exactly-one-ledger-entry rule + maintains `state/status.json`; builder scaffolds new prototypes from `templates/`. (2) `FACTORY.md` — jaw-dropping Mission + Idea criteria rewrite, "Research — the dossier and the wiki" section, Layout (wiki/hub/templates/status.json/reports-assets/research), hub-presentable + dossier-consumption + coroner-budget Quality-bar lines, step-1/5/7 updates (status.json read/verify, wiki verify, show-don't-tell reports). (3) `wiki/` — WIKI.md schema (Karpathy LLM-wiki pattern) + index + log + 12 seed pages. (4) `hub/` — stdlib portal server on 127.0.0.1:7777 (play games, run app demos from status.json, reports/READMEs), traversal-guarded, smoke-tested live. (5) `templates/` — app-python + game-html scaffolds (docs-consistency test; self-checked offline/dead-code scans). (6) `rules/lessons.md` — 25-line checklist + appendix restructure. (7) `state/status.json` seeded for all 5 prototypes.
- **Why:** Elliot's directives (see `directives/archive.md`, 2026-07-05 entries): jaw-dropping idea bar; research that grades ideas before build and deepens the chosen build; research stored per-project and compounded into a wiki that feeds ideation; one place to see/play/try everything; plus the already-approved review items #2–#5. Applied in one session at his explicit request so the next heartbeat runs with the whole pipeline in place rather than drip-applying over five heartbeats.
- **Commit:** (this session's factory-repo commits — see git log, 2026-07-05 evening)
- **Status:** validated (improve-mode path) — this 2026-07-05T22:06 heartbeat ran the new pipeline end-to-end with no phase crashing. (a) The session read `state/status.json` + the lessons checklist for mode/target selection, and the workflow's **Dossier** phase executed and landed `portfolio/chainfall/research/2026-07-05T2206-dossier.md` (a right-sized cascade-animation-feel dossier), which the builder demonstrably consumed — its timing constants, the flash-then-settle skeleton, and the "compress the ~1.1s clone budget ~4-6×" heuristic all landed in the shipped animation. (c) The scribe half needed hand-completion ONLY because the verifier hung (an unrelated defect, now fixed) — but the pipeline's phases themselves ran; the session completed the scribe/ingest work the hung run left, producing exactly one ledger entry and a valid `status.json`. The **Ingest** phase's intent was fulfilled (wiki index + log touched, a new concept page created). (d) Coroner budget was honored (the one caught failure — the charset mojibake — earned exactly one durable check, a static invariant, plus template hardening; no sibling-guard family). (b) The new-mode Ideation/Validate path was NOT exercised (this was improve mode), so that specific branch stays to be confirmed on the next `new` run — but the always-on phases (Dossier, build-consumes-dossier, Ingest, scribe/status.json, coroner budget) all fired correctly. The additive pipeline coexisted with an improve run without friction; not reverted.

### 2026-07-05 — Playtest lens gets an animation-capture reality note + a hard stop-budget
- **Change:** `workflows/heartbeat.js` — extended the game-only 4th "playtest" verify lens (the one added in the prior evolution). Added, inline in the lens prompt: (1) an **ANIMATION-CAPTURE REALITY** note — headless Chromium has no vsync so `requestAnimationFrame` never fires on its own and Playwright throttles/pauses background timers between tool calls, so a multi-frame animation CANNOT be captured by "wait then screenshot", and the verifier must NOT try to force deterministic frame-by-frame capture (fake `performance.now` clocks, manual `setTimeout` pumps, repeated re-navigations — "this exact rabbit hole has sunk >2 hours of a single heartbeat"); instead capture the ONE frame the page happens to show after triggering an action and rely on the pure per-step frames' UNIT TESTS for animation correctness; (2) a **BUDGET / HARD STOP** — the moment the verifier has a handful of distinct-state screenshots plus the zero-console-error + zero-external-request assertions, it is DONE and must stop immediately, not iterate to improve capture quality; (3) a one-line **mojibake eyeball** (garbled em-dashes/arrows like `â€"` → a missing `<meta charset="utf-8">`), harvested from this run's coroner fix, so the playtest actively looks for the encoding defect the static charset invariant now guards. `node --check` passed.
- **Why:** THE friction of this very run, first-hand and severe. This heartbeat's playtest verifier (the lens the previous evolution added) built and drove chainfall correctly and captured usable screenshots — but with no stop-budget and no knowledge that headless browsers can't advance a `requestAnimationFrame` animation, it spent **~2¾h** fighting Playwright timer-throttling trying to film a deterministic cascade, then hung; the heartbeat session had to detect the runaway, stop the workflow, and salvage the back half by hand. The lens's job (drive a real-browser playtest, catch feel/console/network defects) was already met within the first few minutes — everything after was pure waste. This closes the class at the source: the next game verify is told the capture limit up front and given an explicit "you're done, stop" condition, so it can't repeat the sink. Deliberately a PROMPT-ONLY, additive change to the one lens (no control-flow change, trivially reversible), and it deepens the existing playtest guard rather than adding a sibling. Fair game (`workflows/heartbeat.js`); the last workflow did NOT crash — it hung in the verifier and the cause is fully understood (headless rAF + timer throttling + no budget) — and it is stopped now, so editing heartbeat.js was safe per the Evolution rules; `node --check` passed after the edit.
- **Commit:** (this heartbeat's factory-repo commit — see git log)
- **Status:** pending — the next GAME heartbeat validates it: confirm the playtest verifier reads the animation-capture note, does NOT attempt fake-clock / timer-pump frame capture, captures a small set of distinct-state screenshots, checks for mojibake, and STOPS promptly after the console/network assertions instead of over-investing (i.e. the game verify completes in a bounded time rather than running multi-hour). A partial signal now is that the exact 2¾h sink this addresses was diagnosed and fixed this run. **(2026-07-06T02:06 app heartbeat — still pending, not yet validatable:** this was an IMPROVE run on `settleup`, an APP, so `verify()` used the 3 app lenses and the game-only 4th "playtest" lens — the one this change edits — never fired. `workflows/heartbeat.js` DID run end-to-end cleanly this heartbeat (`shipped: true`, no crash), so the added prompt text introduced no regression on the app path, but the specific game-playtest branch is still unexercised. Round-robin polish next selects `subscription-sleuth` then `tidy` (both apps); the first game target after that — groupline or chainfall — is the run that will definitively validate this. Left pending, unchanged, exactly as its own text prescribes.)_

### 2026-07-06 — Scribe dashboard instruction enumerates the footer "Last heartbeat" stamp (close a drift I hand-repaired this run)
- **Change:** `workflows/heartbeat.js` — rewrote the scribe prompt's dashboard instruction (step 3). It previously said only "update its **stats and cards** match INDEX.md and the ledger" and pointed at the card href; it now enumerates EVERY dynamic region the scribe must refresh: (a) the three STATS numbers (prototypes / at-v1-or-beyond / heartbeats-run, the last equal to the ledger entry count), (b) this prototype's card — status pill, description, next-step, and href pointed at this run's report, and (c) **the footer's "Last heartbeat:" stamp, set to `${TS}`** — with an explicit note that (c) "is neither a stat nor a card, so it is the region most often left stale — set it every run."
- **Why:** A manual repair the scribe needed THIS run (the "a manual repair the scribe keeps needing" evolution trigger). This heartbeat's scribe correctly updated `dashboard.html`'s settleup card, its href, and the STATS trio, but left the footer's "Last heartbeat" stamp at the PREVIOUS run's `2026-07-05T22:06:41+01:00` instead of this run's `2026-07-06T02:06:10+01:00`; I hand-repaired it at step 5 and noted it in the ledger. Root cause is precise: the old instruction named "stats and cards", and the footer stamp is neither — so it fell through the enumeration and rot silently (the 22:06 scribe happened to catch it, the 02:06 scribe didn't; the variance is the tell that the instruction was under-specified). Naming the footer stamp explicitly closes that gap at the source, so the next scribe sets it mechanically instead of me re-catching it. Chosen over the top backlog item (per-card "heartbeats spent" + last-updated stamps): that ADDS more scribe-maintained per-card facts, and this run just demonstrated the scribe drops dashboard facts not explicitly enumerated — so tightening the enumeration is the prerequisite, and piling on new un-enumerated facts first would build on sand. Deliberately a PROMPT-ONLY, additive clarification (no control-flow change, trivially reversible), and it deepens the existing dashboard instruction rather than adding a sibling guard. Fair game (`workflows/heartbeat.js`); the last workflow (this run's settleup heartbeat) completed cleanly (`shipped: true`), so editing heartbeat.js was safe per the Evolution rules; `node --check workflows/heartbeat.js` passed after the edit.
- **Commit:** (this heartbeat's factory-repo commit — see git log)
- **Status:** validated — this 2026-07-06T04:06 heartbeat (a `new`/sediment run) consumed the enumerated dashboard instruction and the scribe set `dashboard.html`'s footer "Last heartbeat:" stamp to this run's exact timestamp — `Last heartbeat: 2026-07-06T04:06:37+01:00` — with NO hand-repair needed at step 5, the precise drift this change targets (last run's scribe left it at the prior timestamp and I repaired it by hand). The rest of the enumeration held too: the three STATS numbers were all correct (prototypes 6, at-v1-or-beyond 6, heartbeats-run 17 = the ledger entry count), and the new sediment card's status pill/description/next-step/href all reconciled with INDEX.md — so step 5 needed zero dashboard repairs this run, versus the footer repair last run. The enumeration was consumed mechanically exactly as intended; not reverted. (This validation is what unblocked THIS run's evolution — the per-card "heartbeats spent"/last-updated depth item, below — which the prior entry explicitly sequenced AFTER this enumeration fix so the new per-card facts land on a tightened, non-rotting instruction rather than on sand.)

### 2026-07-06 — Dashboard per-card depth: "N heartbeats · updated <date>" meta line on every card
- **Change:** `portfolio/dashboard.html` + `workflows/heartbeat.js`. (1) Dashboard: added a `.card .meta` CSS rule and a `<div class="meta"><b>N</b> heartbeat(s) · updated YYYY-MM-DD</div>` line under every card's `<h2>` — N = that prototype's count of ledger entries (headings naming it), the "updated" date = the YYYY-MM-DD of its status.json `last_advanced`. Populated all six cards now (tidy 5, groupline 4, subscription-sleuth 3, settleup 2, chainfall 2, sediment 1 — summing to 17, the "heartbeats-run" STAT). Updated the card-template comment to document the meta line, the sum-equals-STAT coherence check, the singular-"heartbeat"-when-1 rule, and the two sources (ledger count + `last_advanced`). (2) Scribe prompt (step 3, region (b)): the scribe must now also refresh the touched card's `<div class="meta">` — increment the bolded count to include this run's just-appended ledger entry and set the "updated" date to `${TS}`'s date (singular when 1) — with an explicit note that a card touched this run is exactly the one whose count just went up, plus a coherence check that the per-card meta counts across all cards sum to the heartbeats-run STAT.
- **Why:** Top of the Proposed backlog (item #1, "the preferred next pick"), and its prerequisite was validated THIS same run: the 2026-07-06 footer-enumeration fix (marked validated above) tightened the scribe's dashboard instruction so newly-added per-card facts won't silently rot — the prior entry explicitly sequenced this depth item AFTER that fix "so the new per-card facts land on a tightened, non-rotting instruction rather than on sand." With the enumeration proven mechanical this run (zero dashboard repairs at step 5), building on it is now safe. The depth is real portfolio-legibility value at near-zero cost: a glance at the dashboard now shows which prototypes have been worked most and when each was last touched — surfacing the round-robin-polish rotation (least-recently-advanced) visually — and the per-card counts summing to the heartbeats-run STAT is a FREE coherence check that catches a scribe miscount. Both facts come from sources the scribe already maintains (the ledger it just appended to; status.json's `last_advanced`), so no new source of truth is introduced. Chosen over the observed friction this run (the `validate:cellblame` StructuredOutput retry-cap failure): that failure's outcome was unharmed (Sediment shipped) and I lack the actual failed-output root cause, so a schema change would be speculative and risks weakening the strong/weak validation discipline the selector depends on — I logged it as backlog item #1 to fix once root-caused or if it recurs, rather than guess now. Fair game: `portfolio/dashboard.html` (a scribe-maintained catalog surface — hand-edited here to seed the feature, with the scribe instruction updated so it stays maintained) + `workflows/heartbeat.js` (the scribe prompt). This run's workflow completed cleanly (`shipped: true`, no crash — the one tolerated `parallel[0]` agent failure is not a workflow crash), so editing `heartbeat.js` was safe per the Evolution rules; `node --check workflows/heartbeat.js` passed after the edit. Small, additive (no control-flow change; new display-only facts), reversible in one commit.
- **Commit:** (this heartbeat's factory-repo commit — see git log)
- **Status:** validated — this 2026-07-06T06:06 heartbeat (an `improve` run on subscription-sleuth) consumed the updated step-3 instruction and the scribe refreshed the touched card's `<div class="meta">` line with NO hand-repair at step 5: subscription-sleuth's meta count incremented **3 → 4** (to include this run's just-appended ledger entry) and its "updated" date was set to this run's date **2026-07-06** — exactly what the instruction prescribes. The whole-portfolio coherence check holds: the per-card meta counts are tidy 5 · subscription-sleuth 4 · groupline 4 · settleup 2 · sediment 1 · chainfall 2, which **sum to 18**, equal to the heartbeats-run STAT (18 = the ledger entry count) — I independently recomputed each prototype's ledger-heading count and every card matches. So the depth facts landed on the tightened, non-rotting enumeration exactly as the prior footer-enumeration fix (validated last run) set them up to. Not reverted.

### 2026-07-06 — Dossier StructuredOutput can't crash the run: make key_findings optional (root-caused from a live crash)
- **Change:** `workflows/heartbeat.js` — the `DOSSIER` StructuredOutput schema no longer REQUIRES `key_findings` (only `dossier_path` is required now); the field stays available but its description tells the agent to keep it a BRIEF highlight list (3-8 items) because the full detail lives in the dossier FILE at `dossier_path` (which every downstream agent reads). The one script consumer, the `log()` line, now null-guards it (`(dossier.key_findings || []).length`). No other code path reads `dossier.key_findings` — the builder, usefulness verifier, coroner, and Ingest all read the dossier FILE, not the return array.
- **Why:** THE friction of this very run, first-hand and fatal. My first workflow launch (improve/subscription-sleuth) CRASHED: the Dossier agent did all its web research and wrote a complete 27KB dossier file to disk, but when it called `StructuredOutput` to RETURN `{dossier_path, key_findings}`, it repeatedly mangled the tool-call XML while trying to serialize a large `key_findings` array (its own transcript: "I keep accidentally including a stray `</invoke>` inside the `key_findings` parameter body, which corrupts the parse") — 5 identical failures (`root: must have required property 'key_findings'`), hitting the StructuredOutput retry cap, which THROWS. The Dossier result is `await`ed with no null-tolerance, so the throw crashed the entire heartbeat mid-run. This is the StructuredOutput-retry-cap class the Proposed backlog's item #1 already flagged (from the sediment `validate:cellblame` failure) — but where the validate variant is non-fatal (validate agents run in `parallel()` and nulls are `.filter(Boolean)`-ed), the dossier variant is fatal because a single `await`ed schema'd call with a required array has no escape hatch. ROOT CAUSE, established from the transcript (not guessed): payload SIZE — a large REQUIRED array forced through the XML tool-call — not enum/semantic mismatch. The cheapest durable fix that removes the crash at its source is to stop REQUIRING the big array in the RETURN: with `key_findings` optional, the very call the agent kept emitting (with the array dropped by the mangle) now VALIDATES on the first try, so the agent can't burn the retry cap, and nothing of value is lost because the dossier FILE — not the return array — is the real deliverable and is what all downstream phases consume. Recovery path taken per the skill's "fails mid-run" guidance: I applied this fix, `node --check`'d it, and RESUMED the same run (`resumeFromRunId`) — the Brief returned cached, the Dossier re-ran live and this time returned cleanly, and the workflow completed end-to-end (`shipped: true`, 97 tests, feature demonstrated). So the fix is not just theorized — the resumed run is its live proof. Fair game: `workflows/heartbeat.js`. The "don't evolve the crashed file until you understand the crash" rule is HONORED, not bypassed: I diagnosed the exact crash (payload-size XML mangle + no null-tolerance on an `await`ed schema call) from the failed agent's transcript BEFORE editing, made the minimal targeted change, and the successful resume validated it within the same heartbeat. This is the one improvement this heartbeat; I deliberately made NO second heartbeat.js edit. Small, additive (relax one `required` list + a null-guard + a size-steering description), reversible in one commit.
- **Commit:** (this heartbeat's factory-repo commit — see git log)
- **Status:** validated — the next build after the same-run resume was this 2026-07-06T08:06 `improve` run on tidy, and its Dossier phase passed through the relaxed schema cleanly: the Dossier agent returned a valid `DOSSIER` object on **attempt 1** (workflow transcript: `state: done`, `attempt: 1`, `lastToolName: StructuredOutput`) with NO `key_findings` field at all — no retry-cap burn, no crash — and the null-guarded `log()` line printed `dossier: 0 findings -> …/2026-07-06T0806-dossier.md` without error (the `(dossier.key_findings || []).length` guard resolved the absent array to 0 rather than throwing). The build/verify/coroner/ingest/scribe phases then all ran to a clean `shipped: true`. So the exact "return validates first-try even when `key_findings` is dropped/absent" outcome the fix was designed for was exercised head-on this run — the field being ABSENT (not just small) is the strongest form of the case, and it validated with zero friction. Both the schema relaxation and the script null-guard are confirmed working; not reverted. (The `key_findings`-present-and-large path — where the highlights still land — will show up naturally on a future build that curates a big highlight list, but the load-bearing crash-prevention property is now validated.)

### 2026-07-06 — Extend the validated dossier size-fix to its sibling: make the VALIDATION schema's large free-text fields optional
- **Change:** `workflows/heartbeat.js` — the new-mode `VALIDATION` StructuredOutput schema (returned by each per-candidate `validate:<name>` agent) no longer REQUIRES its two LARGE free-text fields. `required` went from `['name', 'novelty', 'prior_art', 'resonance_evidence', 'wow_survives', 'revised', 'verdict']` to `['name', 'novelty', 'wow_survives', 'revised', 'verdict']` — dropping `prior_art` and `resonance_evidence` (the two fields the prompt tells the agent to fill with URLs + research prose, so the two that can grow large). Both stay available as optional properties, and their descriptions now steer the agent to keep them CONCISE (a few URLs + a line each) and note they are optional so a large payload can never crash the return. A short block comment records the rationale and points at this log. Every SMALL field the selector's decision depends on stays required: `name`, the `novelty` enum (clear/crowded/exists), the `wow_survives` boolean, the `revised` scores object, and the strong/weak `verdict` enum — none can bloat the tool-call XML, so the selector's strong/weak discipline is untouched. `node --check workflows/heartbeat.js` passed.
- **Why:** The top-of-backlog item (#1 until this run), and the natural, now-UNBLOCKED extension of the `key_findings` fix validated THIS same heartbeat (directly above). That item's own guidance was "ROOT-CAUSE FIRST… NOTE the dossier-variant root cause turned out to be payload SIZE — a large required [field] mangling the tool-call XML — not enum semantics, so check size first," and it was deferred only "until it recurs or the failed outputs are inspected" — i.e. deferred on an EPISTEMIC uncertainty about the root cause. This run resolves that uncertainty: the sibling dossier variant's payload-SIZE root cause and its fix (make the big field optional so a mangled-and-dropped field still validates first-try) are now VALIDATED, not hypothesized. The observed validate-variant failure fingerprint matches size, not enum semantics: `validate:cellblame` burned all 5 retries and returned null while `validate:Sediment`/`validate:Palimpsest` succeeded on the SAME schema (2026-07-06T04:06 sediment run) — candidate-specific, which is exactly what payload size predicts (research verbosity varies wildly by candidate), whereas an enum mismatch would recur across candidates that map to the same nuanced value. The impact is real even though the failure is NON-FATAL (validate agents run in `parallel()` and nulls are `.filter(Boolean)`-ed, so no crash): a candidate whose validate agent happens to research verbosely is silently dropped from selection, so a genuinely strong idea can lose its shot for a purely mechanical XML reason. Making the two big fields optional removes that at its source. I confirmed the change can't break a downstream consumer: `validationOut` is passed to the selector and wiki Ingest agents only as JSON CONTEXT TEXT (`JSON.stringify({shortlist, validations})`), consumed narratively — the script never does property access like `v.prior_art.x` that would throw on an absent field (the only script-level schema-field access anywhere is `dossier.key_findings`, already null-guarded). Deliberately does NOT touch the enums or the `verdict`/`wow_survives`/`revised` decision fields (the item's explicit red line), and DEEPENS the proven fix's reach to its sibling rather than adding a new sibling guard (coroner-budget-aligned). Fair game: `workflows/heartbeat.js`. Safe to edit this round per the Evolution rules — the last workflow (this run's tidy heartbeat) completed cleanly with no crash (`shipped: true`), and the prior run's crash was fully understood and is the very thing this extends. Small, additive (relax one `required` list + two description tweaks + a comment), reversible in one commit.
- **Commit:** (this heartbeat's factory-repo commit — see git log)
- **Status:** validated — this 2026-07-06T14:07 `new`-mode heartbeat (foldup, game) exercised the Ideation→Validate path head-on, and the relaxed `VALIDATION` schema performed exactly as designed. The game round shortlisted and fully validated multiple candidates — **Foldup** (strong, wow_survives, picked), **Overunder** (weak, wow_survives=false, killed on an arXiv topology result), **Proofgrid** (strong but novelty=crowded) — with NO candidate dropped. A transcript scan across all 17 agents for `must have required property 'prior_art'` / `'resonance_evidence'` found **zero** such errors (the only `must have required property` retries in the entire run were on an UNRELATED build/verify agent's `run_command`/`test_command`/`tests_passing` fields, which retried and then succeeded), so no `validate:<name>` agent burned its retry cap on the now-optional large fields. Both optional fields still arrived **populated** (`prior_art`/`resonance_evidence` present 6× each in the validate outputs — e.g. Overunder's `arXiv 1908.04073` citation and Foldup's Unflip/Paperama resonance), confirming the agent fills them despite optionality. And the selector's strong/weak choice was clean and unaffected — it picked the single strong-and-novel candidate (Foldup) and correctly declined the weak (Overunder) and the crowded (Proofgrid). All three validation sub-criteria met; not reverted.

### 2026-07-06 — Pre-launch disk-headroom guard: check free space before launching, DEFER cleanly when the disk is nearly full
- **Change:** `FACTORY.md` (concurrency-guard section) + the `factory-heartbeat` skill's step 0. Added a "check disk headroom before launching the workflow" guard: the heartbeat session runs `df -h` on the factory volume and, if free space is below **~2 GB**, does NOT launch — it DEFERS by writing a `host disk low` failure entry to `state/ledger.md` (naming the free space and the likely hogs: `~/.claude/session-env`, `~/.claude/projects` transcripts, macOS Time Machine local snapshots), flags it for Elliot and the next heartbeat, removes the lock, and ends. The bullet explicitly forbids deleting anything outside the factory tree to make room (Hard-constraint boundary — Elliot's call), notes the 3h lock-staleness reclaim means deferring can't deadlock the factory, and cites the two real incidents that motivate it. The skill mirrors the check in one line pointing back at FACTORY.md.
- **Why:** THE defining friction of this run, first-hand and severe. This 2026-07-06T14:xx heartbeat began with the host volume at **100% full**: the very first Bash call failed at `mkdir ~/.claude/session-env/<id>` → `ENOSPC`, and every shell command, the workflow's agents, git, and `node` were dead — the heartbeat could do no product work and couldn't even write its own failure entry (atomic Write also hit ENOSPC). Worse, the PRIOR run (2026-07-06T10:06 groupline) had filled the disk DURING its own execution: its product work committed in groupline's repo and its scribe writes landed, but its factory-repo commit died on ENOSPC, orphaning the ledger/INDEX/status/lessons changes uncommitted for ~4h until Elliot freed space — which this session then had to reconcile (reconstruct the lost report, re-commit the orphan, repair-note the ledger). A `df` check costs one cheap command and would have (a) turned this run's total-lockout into a clean, recorded early deferral instead of a hand-written post-mortem, and (b) caught the 10:06 low-disk condition before it did uncommittable work. Chosen over the also-real ledger-read-overflow friction (the ledger is now 21 entries and reading it overflowed the 25k-token tool limit twice this session — the top-backlog "ledger rollup" item, now more pressing at 21/30 and bumped in priority) because the disk guard prevents a *commit-losing* failure, is strictly smaller (a policy bullet + a skill line, no new archive-file machinery), and is the direct lesson of this run's catastrophe. Deliberately does NOT auto-clean disk (Hard constraint forbids touching files outside the factory tree, and blind cache-deletion is Elliot's call) — it only checks and defers, which is the safe, reversible move. Fair game: `FACTORY.md` policy wording + the `factory-heartbeat` skill (one of the two skills evolution may edit). Does NOT touch `workflows/heartbeat.js` (so no crash-risk, and the "don't edit the crashed file" rule is moot — nothing crashed heartbeat.js; the workflow this run completed cleanly, `shipped: true`). Small, additive, reversible in one commit.
- **Commit:** (this heartbeat's factory-repo commit — see git log)
- **Status:** pending — validated by the next heartbeat that either (a) starts on a low-disk volume (<~2 GB free) and correctly DEFERS with a `host disk low` ledger entry rather than launching a doomed workflow, or (b) starts with ample free space, runs `df -h`, sees it's above the threshold, and proceeds normally with no friction (the common case — confirms the added check is a cheap no-op that doesn't impede healthy runs). Because the guard lives in the pre-launch procedure the heartbeat session executes directly (like the lock check), every future heartbeat exercises the healthy-path branch (b) immediately; branch (a) validates only if/when disk pressure recurs.
